# Runtime dependencies for kr6-kinematics
# (the canonical specification lives in pyproject.toml — this file is
# provided as a convenience for users who prefer `pip install -r`)

numpy>=1.23
matplotlib>=3.6
spatialmath-python>=1.1
urchin>=0.0.27
pillow>=9.0
